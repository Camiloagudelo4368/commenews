{{!-- <div class="row justify-content-center">
    <div class="col">
        <img src="/assets/img/burger.png">
    </div>
</div> --}}
{{!-- <div class="row justify-content-center">
    <div class="col-lg-8 col-md-10 col-sm-12 col-xs-12">
        <br>
        <form action="/api/burger" method="post">
            <div class="input-group mb-3">
                <input name="burger_name" type="text" class="form-control" placeholder="Enter your most desired burger"
                    required aria-label="Type your most wished burger" aria-describedby="button-addon2">
                <div class="input-group-append">
                    <button class="btn btn-outline" type="submit" id="btnSubmit"><img
                            src="/assets/img/plus.svg"></button>
                </div>
            </div>
        </form>
    </div>
</div> --}}
<br>
{{#each articles}}
<div class="row">
    <div class="row portfolioRow">
        <div class="col-lg-5 col-md-5 col-sm-12 col-xs-12 text-right">
            <img class="card-img-top portfolio_image" src="{{img}}">
        </div>
        <div class="col-lg-7 col-md-7 col-sm-12 col-xs-12">
            <h3 class="portfolioHeader"><a href="{{link}}">{{title}}</a></h3>
            <p class="portfolioDescription">{{description}}</p>
            <button type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#modelId">
                Comment
            </button>
        </div>
    </div>
    <div class="row portfolioRow">

    </div>
</div>
{{/each}}
<br>
<br>
<br>
<div class="container">
    <div class="row">
        <div class="col-lg-12 footer_style align-middle">
            <footer>Copyright 2019© Camilo Agudelo</footer>
        </div>
    </div>
</div>

<!-- Button trigger modal -->


<!-- Modal -->
<div class="modal fade" id="modelId" tabindex="-1" role="dialog" aria-labelledby="modelTitleId" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Comments</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="container-fluid">
                    {{#each articles}}
                    <div class="row">
                        <div class="row portfolioRow">
                            <div class="col-lg-5 col-md-5 col-sm-12 col-xs-12 text-right">
                                <img class="card-img-top portfolio_image" src="{{img}}">
                            </div>
                            <div class="col-lg-7 col-md-7 col-sm-12 col-xs-12">
                                <h3 class="portfolioHeader"><a href="{{link}}">{{title}}</a></h3>
                                <p class="portfolioDescription">{{description}}</p>
                                <button type="button" class="btn btn-primary btn-sm" data-toggle="modal"
                                    data-target="#modelId">
                                    Comment
                                </button>
                            </div>
                        </div>
                        <div class="row portfolioRow">

                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save</button>
            </div>
        </div>
    </div>
</div>